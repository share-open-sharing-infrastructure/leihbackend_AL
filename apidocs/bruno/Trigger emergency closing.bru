meta {
  name: Trigger emergency closing
  type: http
  seq: 6
}

post {
  url: {{API_BASE}}/misc/emergency_closing
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "identity": "{{USERNAME}}",
    "password": "{{PASSWORD}}"
  }
}

vars:post-response {
  token: res.body.token
}

script:pre-request {
  await bru.runRequest('Authenticate')
}

settings {
  encodeUrl: true
  timeout: 0
}
